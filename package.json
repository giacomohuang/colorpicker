{
  "name": "@mcistudio/colorpicker-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -F colorpicker dev",
    "dev:docs": "pnpm -F docs docs:dev",
    "build:docs": "pnpm -F docs docs:build",
    "build": "pnpm -F colorpicker build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
